name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
      - preview/prism-v3-stage3
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Run repository tests
        run: npm test
      - name: Test and build Prism V3 Stage 3 preview
        if: github.ref == 'refs/heads/preview/prism-v3-stage3'
        working-directory: apps/prism-game-v3
        run: |
          npm install
          npm test
          npm run build

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        if: github.ref == 'refs/heads/preview/prism-v3-stage3'
        with:
          node-version: 22
      - name: Build Stage 3 browser preview
        if: github.ref == 'refs/heads/preview/prism-v3-stage3'
        working-directory: apps/prism-game-v3
        run: |
          npm install
          npm run build
      - name: Place Stage 3 preview at static subpath
        if: github.ref == 'refs/heads/preview/prism-v3-stage3'
        run: |
          rm -rf prism-v3-stage3-preview
          mkdir -p prism-v3-stage3-preview
          cp -R apps/prism-game-v3/dist/. prism-v3-stage3-preview/
          rm -rf apps/prism-game-v3/node_modules
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload static repository
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
